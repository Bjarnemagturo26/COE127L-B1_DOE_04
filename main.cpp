#include <string>
#include "Player.h"

int main()
{
std::string name2;
std:: string str;
int score1;
int score2;

std::getline(ifs,name1, ';');
std::getline(ifs,str, '\n');
score1 = atoi(str,c,str());

std::getline(ifs,name2, ';');
std::getline(ifs,name2, '\n');
score2= atoi9str,c-str());

game.play(name1,score1,name2,score2);

return 0;
}
